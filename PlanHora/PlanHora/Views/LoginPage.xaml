<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlanHora.Views.LoginPage"
             Title="PlanHora - Inicio">
    <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
        <Label Text="Bienvenido a PlanHora" FontSize="24" HorizontalOptions="Center" />
        <Entry x:Name="UsernameEntry" Placeholder="Usuario" />
        <Entry x:Name="PasswordEntry" Placeholder="Contraseña" IsPassword="True" />
        <Button Text="Iniciar sesión" Clicked="OnLoginClicked" />
        <!-- Nuevo botón de registro -->
        <Button Text="Registrarse" Clicked="OnRegisterClicked" />
        <!-- botón para fase de desarollo para pruebas -->
        <Button Text="Entrar sin registro" Clicked="OnSkipLoginClicked" />
    </VerticalStackLayout>
</ContentPage>